<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionOverflowButtonStyle">@style/ToolbarOverflowTheme</item>
        <item name="android:listDivider">@color/colorDivider</item>
        <item name="actionBarTheme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
        <item name="alertDialogTheme">@style/AlertDialogCustom</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ToolbarOverflowTheme" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:src">@drawable/ic_toolbar_question</item>
    </style>

    <style name="ToolbarNavigationTheme" parent="Widget.Design.NavigationView">
        <item name="android:src">@drawable/ic_toolbar_navigation</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <!-- Right menu actions (about, buy, exit) settings -->
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:colorBackground">@color/colorPrimary</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:divider">@drawable/divider</item>
        <item name="android:listDivider">@color/colorDivider</item>
    </style>

    <style name="AlertDialogCustom" parent="@android:style/Theme.Holo.Dialog.NoActionBar">
        <!-- @android:style/Theme.Holo.Dialog.NoActionBar, Theme.DeviceDefault.Light.Dialog as parent looks like good -->
        <item name="android:textColorPrimary">@color/colorText</item>

        <!-- to clear border behind (mb of parent fragment) -->
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- to clear border behind (mb of parent fragment) -->

        <item name="android:typeface">sans</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="Font" parent="@android:style/TextAppearance.Medium">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#00FF00</item>
        <item name="android:typeface">monospace</item>
    </style>

</resources>
